# coding: utf-8
testLine = ''.join('%s %1.6f %1.6f %1.6f' % (aelem[0], x[0], y[0], z[0]))
get_ipython().magic('edit ipython-session.py')
data
x
for a in data[:,0]:
    print a
    
for a in data[:,0]:
    print(a
    )
    
for a in data[:,0]:
    print(a)
    
for a,b,c  in data[:,0],data[:,1],data[:,2]:
    print(a)
    
for a,b,c  in data[:,0],data[:,1],data[:,2]:
    print(a,b,c)
    
data[:,0]
data[:,2]
data[:,3]
data[:,2]
data[:,1]
data.shape
for (a,b,c)  in data[:,0],data[:,1],data[:,2]:
    print(a,b,c)
    
for (a,b,c)  in (data[:,0],data[:,1],data[:,2]):
    print(a,b,c)
    
for i,a in enumerate(data[:,0]):
    print i
    
for i,a in enumerate(data[:,0]):
    print (i)
    
for i,a in enumerate(data[:,0]):
    print (i,a)
    
for i in enumerate(data[:,0]):
    print (i)
    
for i,a in enumerate(data[:,0]):
    print (i)
    
for i,a in enumerate(data[:,0]):
    print (data[i,:])
    
for i,a in enumerate(data[:,0]):
    print (elem[i],data[i,:])
    
for i,a in enumerate(data[:,0]):
    print (aelem[i],data[i,:])
    
for i,a in enumerate(data[:,0]):
    print (aelem[i],data[i,:])
    
get_ipython().magic('edit ipython-session.py')

employee_emails = ["Wile.E.Coyote@acme.com", 
                   "Looney.Tunes@acme.com", 
                   "Chuck.Jones@acme.com", 
                   "Road.Runner@acme.com", 
                   "Michael.Maltese@acme.com", 
                   "Speedy.Gonzales@acme.com",
                   "Calamity.Coyote@acme.com", 
                   "Bugs.Bunny@texavery.com"]
employee_ids = range(8)
print employee_ids
employee_emails = ["Wile.E.Coyote@acme.com", 
                   "Looney.Tunes@acme.com", 
                   "Chuck.Jones@acme.com", 
                   "Road.Runner@acme.com", 
                   "Michael.Maltese@acme.com", 
                   "Speedy.Gonzales@acme.com",
                   "Calamity.Coyote@acme.com", 
                   "Bugs.Bunny@texavery.com"]
employee_ids = range(8)
print employee_ids
aelem = np.genfromtxt('STO-110.xyz',skiprows=2, usecols=[0],dtype=np.str)
alem
aelem = np.genfromtxt('STO-110.xyz',skiprows=2, usecols=[0],dtype=np.str)
aelem
for i,a in enumerate(data[:,0]):
    print (aelem[i],data[i,:])
    
for i,a in enumerate(data[:,0]):
    print (aelem[i],data[i,1], data[i,2], data[i,3])
    
for i,a in enumerate(data[:,0]):
    print (aelem[i],data[i,0], data[i,1], data[i,2])
    
open
get_ipython().system('cat xyz2cif.py')
for i,a in enumerate(data[:,0]):
    print (elem[i],data[i,:])
    
for i,a in enumerate(data[:,0]):
    print ('%s %1.10f' %elem[i],data[i,])
    
for i,a in enumerate(data[:,0]):
    print ('%s %1.10f' % (elem[i],data[i,1]))
    
for i,a in enumerate(data[:,0]):
    print ('%s %1.10f' % (aelem[i],data[i,1]))
    
import xyz2cifnp as xyz
xyz.convert2cif('STO-110-Surf-O.xyz')
import xyz2cifnp as xyz
xyz.convert2cif('STO-110-Surf-O.xyz')
del xyz
import xyz2cifnp as xyz
xyz.convert2cif('STO-110-Surf-O.xyz')
get_ipython().system('cat STO-110-Surf-O.xyz')
get_ipython().system('cat STO-110-Surf-O.xyz_coords.cif')
xyz.convert2cif('STO-110-Surf-O.xyz')
xyz.convert2cif('STO-110-Surf-O.xyz')
del xyz
import xyz2cifnp as xyz
xyz.convert2cif('STO-110-Surf-O.xyz')
data = np.loadtxt('STO-110-Surf-O.xyz', skiprows=2, usecols=(1,2,3))
data
data[:,0]
del x, y, z
del x
del y
del z
xyz.convert2cif('STO-110-Surf-O.xyz')
data [:,0]
data [:,0].shape
edata = np.genfromtxt('STO-110-Surf-O.xyz',skiprows=2, usecols=(0), dtype=str)
